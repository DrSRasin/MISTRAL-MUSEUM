# CLAUDE.md — MISTRAL-MUSEUM

Cold start: read `TRUST_BUT_VERIFY.md` FIRST — it is the binding anti-amnesia log for this repo.

- Push only on operator GO.
- Do not mix unrelated jobs in one commit.
- Shared tree: concurrent agent sessions may hold uncommitted edits. Check `git log -5` + `git status` before any commit.
- License: AGPL-3.0-only.
